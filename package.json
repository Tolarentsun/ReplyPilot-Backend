{
  "name": "replypilot-backend",
  "version": "1.3.0",
  "description": "AI-Powered Review Response Platform for Local Businesses",
  "main": "server.js",
  "type": "commonjs",                    // Keep this for now (you are using require/module.exports)
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ext .js",       // optional but recommended
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7",
    "axios": "^1.8.0",
    "stripe": "^17.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
