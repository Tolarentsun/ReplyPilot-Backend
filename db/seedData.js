// No demo data seeded for new users
// Users start with an empty dashboard and add their own reviews

async function seedDemoReviews(userId) {
  // Intentionally empty - real customers add their own reviews
  console.log('New user created:', userId);
}

module.exports = { seedDemoReviews };
