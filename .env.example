# Optional overrides. All inference is billed through Netlify AI Gateway by default.

# JWT signing secret — set this in Netlify env for production.
JWT_SECRET=change-me-in-production

# Override AI provider keys only if you don't want to use Netlify AI Gateway.
# ANTHROPIC_API_KEY=
